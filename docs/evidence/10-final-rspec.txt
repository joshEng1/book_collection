
Randomized with seed 18111

User Books
  creates, shows, updates, and deletes an association
  makes User Books the home page with navigation
  rejects missing foreign records without a server error
  rerenders invalid and duplicate associations with errors and populated dropdowns
  uses dropdowns containing available users and books

UserBook
  prevents duplicate associations
  allows the same book to belong to multiple users
  requires an existing user and book
  removes associations when a book is deleted while retaining users

User
  removes associations when deleted while retaining books
  reaches many books through user books
  requires a username

Users
  rejects blank usernames on create and update
  creates, lists, shows, updates, and deletes a user

Managing the collection in a browser
  creates, reads, updates, cancels deletion, and deletes a book
  shows a blank-title validation error
  manages a user and their book association with dropdowns

Books
  creates a titled book and displays a success notice on the home page
  rejects a blank title with an error notice and preserves the database
  saves and displays the price from the form
  saves a published date from dropdowns and displays it
  saves and displays the author from the form

Book CRUD
  requires a DELETE request and shows a home-page deletion notice
  does not change a record on an invalid update
  rejects an incomplete publication date with validation errors
  rejects invalid prices without writing to the database
  provides all five book pages and a home link
  returns all book attributes through the existing JSON API
  updates all attributes and shows a home-page notice
  returns 404 for missing records
  prepopulates edit fields and uses three publication date dropdowns

Book
  stores a decimal price without losing cents
  stores an author
  rejects impossible publication dates from dropdowns
  accepts a title
  rejects a blank title
  rejects invalid publication date strings
  stores a published date

Finished in 7.5 seconds (files took 7.68 seconds to load)
38 examples, 0 failures

Randomized with seed 18111

